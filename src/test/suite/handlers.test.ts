import * as assert from 'assert';
import * as vscode from 'vscode';

suite('handlers', () => {
  describe('up', () => {
    test('run pane commands defined in given itermocil layout file', () => { })
    test('if no workspace is opened fails', () => { })
    test('no layout file is given fails', () => { })
    test('if given file does not exist fails', () => { })
  })
})